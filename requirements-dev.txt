# Local development + CI only (not installed on Railway by default).
django-debug-toolbar>=4.2
pytest>=7.4
pytest-django>=4.5
# crewai removed — was never imported. Orchestrator uses native
# ThreadPoolExecutor-based parallel execution (see api/ai/agents/orchestrator.py).
