# Keep in sync with ../requirements.txt (Railway / root production install).

Django>=4.2,<5
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-otp>=1.4
pyotp>=2.9
python-decouple>=3.8
# Postgres (Neon / production): driver + DATABASE_URL parsing
psycopg[binary]>=3.1
dj-database-url>=2.1
# Optional: Redis cache/sessions, file uploads (Pillow)
redis>=5.0
Pillow>=10.0
# PDF export (patient record)
reportlab>=4.0
# Real-time alerts (Django Channels)
channels>=4.0
channels-redis>=4.2
daphne>=4.0
# AI / ML (xgboost<3 avoids large CUDA/NVIDIA wheels on Linux CPU hosts)
scikit-learn>=1.3.0
xgboost>=2.0.0,<3
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0
# Async Task Queue (Celery) and Message Broker
celery>=5.3.0
django-celery-beat>=2.5.0
django-celery-results>=2.5.0
kombu>=5.3.0
django-cryptography>=1.1
