# Vercel serverless: keep total installed size under Lambda's ~500 MB limit.
# Full stack (incl. scikit-learn / xgboost / pandas) stays in requirements.txt for Railway.
# AI endpoints that need ML wheels should run on Railway or use a separate service.

Django>=4.2,<5
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-otp>=1.4
pyotp>=2.9
python-decouple>=3.8
psycopg[binary]>=3.1
dj-database-url>=2.1
redis>=5.0
Pillow>=10.0
reportlab>=4.0
channels>=4.0
channels-redis>=4.2
daphne>=4.0
celery>=5.3.0
django-celery-beat>=2.5.0
django-celery-results>=2.5.0
kombu>=5.3.0
django-cryptography>=1.1
